name: CI Workflow
on: 
    push:
      branches:
        - main
        - develop
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Step
        run: echo "Building ..."
      - name: Deploy Step
        if: success()
        run: echo "Deploying"             